#Task1
#The greatest number

import random
list = random.sample(range(1,101),10) #generates list of 10 random numbers
print (max(list), list)


#Refactored Task1
import random
list = []
i = 0
while i<=758:
     i+=12
     list.append(i) #generated sequence of numbers using while loop and indexes appending them to our empty list
     #problem - this list is not random but sequential with a very clear step 12, but it does satisfy condition of only using while loop
      #we need to meet condition of length of the list and make it random, therefore:
list2 = random.sample(list,10) #using random module we rearrange our list into a new one to make it truly random where number of steps sampling our list meets condition of length of our list being 10 numbers
#for sanity check if we put print(list2) we get this short, new, truly random list
print(max(list2)) #using max function choose max from the list of random numbers



#Task2
#Exclusive common numbers.

import random

list1 = random.sample(range(1,11),10)#list1 of 10 numbers in random order
list2 = random.sample(range(1,11),10) #llist2 of 10 numbers in random order
list3 = set(list1) & set(list2) #common numbers in both lists without duplicates

print (list(list3))



#task3
#Extracting numbers
ourlist = []
i = 1
while i <=100: 
   i+=1
   if i % 7 == 0 and i % 5 != 0: #every value is divisible by 7 but not a multiple of 5
       ourlist.append(str(i))

       print(ourlist)

#Refactored
list = []
list1 = []
i = 1
while i <=100: 
   i+=1
   list.append(i)
   if i % 7 == 0 and i % 5 !=0:
        list1.append(i)
        print(list1)



